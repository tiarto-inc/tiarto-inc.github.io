<svg xmlns="http://www.w3.org/2000/svg" width="160" height="28" viewBox="0 0 160 28" fill="none">
  <!-- Simple wordmark-style logo (white) — replace this file with your official SVG when ready -->
  <style>
    .word { fill: #ffffff; }
    @media (prefers-color-scheme: light) {
      .word { fill: #000000; }
    }
  </style>
  <g class="word" font-family="Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial" font-size="20" font-weight="700">
    <text x="0" y="20" letter-spacing="0.5">tiarto</text>
  </g>
  <!-- Accent underline -->
  <rect x="0" y="24" width="64" height="2" fill="#FF00A8"/>
</svg>
